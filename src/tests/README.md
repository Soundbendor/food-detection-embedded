# Tests

This is for python scripts that allow individual testing of specific components

All test scripts must be run using the following syntax from the root directory of the project
```
run_test src.tests.TEST_NAME
```
